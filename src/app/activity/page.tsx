import Image from "next/image";
import { activities, reviews } from "@/data/media";
export const metadata={title:"Activity"};
export default function Page(){return <div className="page"><div className="page-narrow"><header className="page-hero"><span className="eyebrow">Your circle</span><h1>Activity</h1><p>New ratings, reviews, completions, and progress from people you follow.</p></header><section className="section"><div className="feed-grid"><div className="panel">{activities.concat(activities).map((a,i)=><div className="activity-row" key={`${a.id}-${i}`}><Image className="avatar" src={a.user.avatarUrl} width={42} height={42} alt=""/><div className="activity-copy"><strong>{a.user.displayName}</strong> shared an update about<br/><strong>{a.media?.title}</strong> {a.rating&&<span className="rating">★ {a.rating}</span>}</div><span className="activity-time">{a.createdAt}</span></div>)}</div><div className="panel" style={{padding:20}}>{reviews.map(r=><blockquote key={r.id} style={{margin:"0 0 24px",color:"var(--foreground-secondary)",lineHeight:1.6}}>“{r.body}”<footer className="muted" style={{marginTop:8,fontSize:12}}>{r.user.displayName} · <span className="rating">★ {r.rating}</span></footer></blockquote>)}</div></div></section></div></div>}
